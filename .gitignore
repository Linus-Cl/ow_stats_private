# Local runtime databases
active_sessions.db
*.db-journal

# Local JSONL data store (dev test data – never push to repo)
local_data.jsonl
local_data.jsonl.tmp

# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# OS files
.DS_Store

# Ignore Excel files
*.xlsx

# Optional: Ignore OS files
.DS_Store
__pycache__/

# Local prompts and automation (do not track)

# Firebase credentials (NEVER commit)
firebase-credentials.json

# Legacy/untracked helper files (ignore if they reappear locally)
assets_utils.py
branding.py
patchnotes_utils.py
sessions.py
scripts/cloudflare_worker.js
.prompts/